import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const FavoriteModule = () => {
  return (
    <SafeAreaView>
      <Text>FavoriteModule</Text>
    </SafeAreaView>
  )
}

export default FavoriteModule