import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const SearchModule = () => {
  return (
    <SafeAreaView>
      <Text>SearchModule</Text>
    </SafeAreaView>
  )
}

export default SearchModule